<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Date</th>
      <th>Time</th>
      <th>Location</th>
      <th>Room</th>
      <th>Contact Name</th>
      <th>Contact Phone</th>
      <th>Contact Email</th>
      <th>Max Seats</th>
      <th>Remaining Seats</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @event.name %></td>
      <td><%= @event.date.to_date.strftime('%b %e, %Y') %></td>
      <td><%= @event.time %></td>
      <td><%= @event.location.name %></td>
      <td><%= @event.room %></td>
      <td><%= @event.contact_name %></td>
      <td><%= @event.contact_phone %></td>
      <td><%= @event.contact_email %></td>
      <td><%= @event.max_seats %></td>
      <td><%= @event.remaining_seats %></td>
    </tr>
  </tbody>
</table>

<!-- Button to trigger modal -->
<a href="#myModal" role="button" class="btn btn-primary" 
   data-toggle="modal">Add Attendee</a>
 
<!-- Modal -->
<div id="myModal" class="modal-700 hide fade" tabindex="-1" 
     role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Add Attendee</h3>
  </div>
  <div class="modal-body">
    <div class="form-horizontal">
      <form action="<%= event_regs_url %>" method="post">
            <%= render partial: "reg_form" %>
            <button class="btn" data-dismiss="modal" aria-hidden="true">
              Close
            </button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>
<br>
<br>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>DOB</th>
      <th>Attended</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @event_regs.each do |event_reg| %>
    <tr>
      <td>
        <% if event_reg.idn %>
          <%= event_reg.idn %>
        <% else %>
          <%= render partial: "edit_form", :locals => {event_reg: event_reg} %>
        <% end %>
      </td>
      <td><%= event_reg.name %></td>
      <td><%= event_reg.dob %></td>
      <% if event_reg.idn %>
      <td>
        <form action="<%= event_reg_url(event_reg) %>" method="post">
          <input type="hidden" name="_method" value="put">
          <input type="hidden" name="authenticity_token" 
           value="<%= form_authenticity_token %>">
          <label class="radio" style="float:left; width:35px">
          <input type="radio" name="event_reg[attended]" id="event_reg_no" 
                 value="false" <%= 'checked' unless event_reg.attended %>
                 >No</label>
          <label class="radio" style="float:left; width:35px">
          <input type="radio" name="event_reg[attended]" id="event_reg_yes" 
                 value="true" <%= 'checked' if event_reg.attended %>
                 >Yes</label> 
          <input type="submit" value="submit" 
                 class="btn btn-mini btn-primary">
        </form>
      </td>
      <% else %>
      <td>id not found</td>
      <% end %>
      <td><%= link_to "edit", edit_event_reg_url(event_reg) %> | 
        <%= link_to "delete", event_reg_url(event_reg), :method => "delete" %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>