<form action="<%= event_url(@event) %>" method="post" 
      class="form-horizontal">
  <input type="hidden" name="_method" value="put">
      <%= render partial: "form" %>
      <button type="button" class="btn btn-default" onClick="location.href='<%= events_url %>'">Cancel</button>
    </div>
  </div>
</form>